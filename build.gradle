buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath group: 'com.cinnober.gradle', name: 'semver-git', version: '2.4.0'
    }
}

plugins {
    id 'java'
    id 'eclipse'
    id 'maven'
}

apply plugin: 'com.cinnober.gradle.semver-git'

group = 'com.github.vic-cw'
